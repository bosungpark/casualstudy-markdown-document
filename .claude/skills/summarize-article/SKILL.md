---
name: summarize-article
description: Research one in-depth technical article or paper and summarize it as a Korean Markdown study note following this repository's 출처 / AI 요약 / 내가 얻은 인사이트 structure. Use for `/summarize-article` with a topic, `$summarize-article`, or requests such as "기술 아티클 찾아서 정리", "이 주제 딥다이브", and "아티클 기반 PoC까지 해보자".
---

# Summarize Technical Article

기술 주제를 다루는 아티클 한 편을 선정해 한국어 학습 문서로 정리한다. 사용자가 요청하면 같은 위치에 재현 가능한 PoC도 만든다.

## 입력

- 명시적 호출의 인자 또는 현재 요청에서 기술 주제를 가져온다.
- 주제가 불명확할 때만 짧게 질문한다.

## 작업 순서

1. 저장소 루트의 `CLAUDE.md`, `AGENTS.md` 등 적용 가능한 작성 규칙을 먼저 확인한다.
2. 인터넷에서 `[주제] architecture deep dive internals`와 같이 검색한다.
3. 공식 문서, 원저자 기술 블로그, 논문 등 가장 깊이 있고 신뢰할 수 있는 **아티클 한 편**을 선택해 원문을 읽는다.
4. 선택한 아티클을 중심으로 핵심 구조, 동작 순서, 트레이드오프를 정리한다.
   - 제목을 질문형·증상형으로 지었다면(`왜 ~인가`, `~하면 무슨 일이 생기는가`) **요약 첫 절에서 그 질문에 직접 답한다.** 원문 목차 순서를 그대로 옮기면 제목과 본문이 따로 논다.
   - 나머지 절은 그 답의 근거가 되도록 배치한다. 답 먼저, 근거 나중.
5. 기존 카테고리 중 가장 가까운 위치를 선택한다.
   - 인프라·클라우드·운영: `infra/`
   - 시스템·분산 시스템: `system_design/`
   - 네트워크·프로토콜: `network/`
   - 데이터베이스: `database/`
   - AI·ML: `ai/`
6. 문서만 만들면 `[주제 제목].md`로 저장한다.
7. 실행 코드나 PoC도 만들면 `[주제 제목]/` 디렉터리를 만들고 문서, `README.md`, 실행 파일을 모두 그 안에 둔다. 별도의 범용 `pocs/` 디렉터리를 만들지 않는다.
8. 링크, 명령어, 코드 문법을 검사한다. PoC를 포함했다면 안전한 격리 환경에서 실제 실행해 핵심 주장을 검증한다.

## 단일 출처 원칙

- 한 문서의 `출처`에는 아티클·논문·챕터 하나만 둔다.
- 여러 아티클의 내용을 하나의 요약처럼 섞지 않는다.
- 보조 자료로 사실을 검증하더라도 선택한 원문의 주장과 자신의 해석을 구분한다.
- 여러 자료를 각각 정리해야 한다면 문서를 분리한다.

## 문서 템플릿

```markdown
# [주제 제목]

## 출처
- **아티클/논문**: [정확한 제목]
- **저자/출처**: [저자 또는 사이트]
- **링크**: [원문 URL]

---

## AI 요약

### 0. [전제가 되는 모델] / [제목이 던진 질문에 대한 답]
[핵심 정의와 범위. 질문형 제목이면 여기서 곧바로 답한다.]

| 제목의 질문 | 한 줄 답 | 원리 | 실측 |
| --- | --- | --- | --- |
| [질문] | [한 줄 답] | [N절] | [PoC X] |

### 1~N. [핵심 개념]
[필요한 표, ASCII 흐름도, 짧은 코드 예시]

---

## PoC - 직접 확인한 것   <!-- 실행 코드를 만들 때만 -->

[시나리오 표(시나리오 / 확인하는 것 / 요약 절) → 시나리오별 명령, 출력, 판정]

---

## 내가 얻은 인사이트

### [관점]
1. **[인사이트 제목]**
   - 원문에서 도출한 실무적 의미와 트레이드오프
```

## 작성 기준

- 전체 설명은 한국어로 쓰고 필요한 기술 용어는 영어를 병기한다.
- 구조·흐름을 이해하는 데 도움이 될 때만 ASCII 다이어그램이나 표를 사용한다.
- 원문 요약과 직접 실험 결과를 명확히 구분한다.
- 시간에 따라 변할 수 있는 버전, 명령어, 지원 상태는 현재 공식 자료에서 확인한다.
- 긴 원문 인용 대신 간결한 의역을 사용한다.
- 출처에 없는 사실을 원문의 주장처럼 쓰지 않는다.
- `원문은 여기서 한 걸음 더 들어간다`, `원문이 지적하는 문제는`, `원문의 결론은 단순하다` 같은 메타 서술을 반복하지 않는다. 주장을 직접 쓰고, 원문과 내 해석을 구분해야 하는 자리에서만 출처를 밝힌다.
- 결론은 비교 가능한 값으로 닫는다. `10.2초 / exit 137 → 0.1초 / exit 143`처럼 전후를 나란히 둔다.

## 자가 점검

문서를 저장하기 전에 아래를 확인한다. 실제로 이 저장소에서 나왔던 결함들이다.

- **제목의 질문에 요약이 답하는가.** 답이 PoC 절에만 흩어져 있고 요약은 원문 목차만 따라가는 경우가 많다.
- **교차 참조가 문서 안을 가리키는가.** `→ 2절`, `→ PoC C`처럼 이 문서의 번호로 쓴다. 원문의 영문 소제목을 참조 열에 두면 독자가 원문으로 튕겨 나가고 요약과의 연결이 끊긴다. 원문 대응표가 필요하면 각주 한 줄로 내린다.
- **용어를 쓰기 전에 정의했는가.** 뒤 절에서 설명할 용어(수확, 재부모화 등)를 앞 절 도식에 먼저 쓰지 않는다.
- **도구·이름이 하나로 통일됐는가.** 원문의 도구를 현대 대체물로 바꿔 검증했다면 본문은 검증에 쓴 쪽으로 통일하고, 전환은 한 문장으로 한 번만 밝힌다. 둘을 번갈아 쓰면 독자는 도구가 몇 개인지부터 헷갈린다.
- **숫자에 근거가 있는가.** 문서에 붙인 출력이나 검증 스크립트에 없는 수치를 쓰지 않는다.
- **개수를 말한 헤딩이 항목 수와 맞는가.** `두 가지 예외` 아래 세 항목이 있으면 둘 중 하나가 틀린 것이다.
- **같은 그림을 두 번 그리지 않았는가.** 요약 절은 원리, PoC는 실측값, 인사이트는 판단으로 역할을 나눈다. 같은 흐름도가 두 절에 나오면 한쪽은 한 문장으로 줄이고 다른 쪽을 가리킨다.
- **검증하지 않은 주장이 검증된 것처럼 보이지 않는가.** PoC로 확인하지 않은 절은 그렇게 밝힌다. 검증한 절과 나란히 두면 전부 실측처럼 읽힌다.
